import { NextResponse } from "next/server"; import prisma from "@/lib/prisma"; export async function GET() { try { await prisma.$executeRawUnsafe(`ALTER TABLE "Meeting" ADD COLUMN IF NOT EXISTS "groupPhoto" TEXT;`); return NextResponse.json({ success: true, message: "Banco de dados sincronizado com sucesso! A coluna groupPhoto foi adicionada." }); } catch (error: any) { return NextResponse.json({ success: false, error: error.message || String(error) }); } }
